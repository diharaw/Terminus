#pragma once

#include <types.h>
#include <ECS/scene.h>

namespace terminus
{
	namespace entity_factory
	{
		inline Entity& create(JsonValue& json, Scene* scene)
		{

		}

		inline Entity& create_from_prefab(String prefab, Scene* scene)
		{

		}
	}
}