#pragma once

#include <types.h>
#include <ECS/scene.h>

namespace terminus
{
    namespace cpp_script_component_factory
    {
        void create(JsonValue& json, Entity& entity, Scene* scene)
        {
            
        }
    }
}

